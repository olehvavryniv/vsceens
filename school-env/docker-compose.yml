services:
    mongo:
        image: mongo
        restart: always
        environment:
            MONGO_INITDB_ROOT_USERNAME: admin
            MONGO_INITDB_ROOT_PASSWORD: password
        ports:
            - 27017:27017
    mongo-express:
        image: mongo-express
        restart: always
        ports:
            - 8081:8081
        environment:
            ME_CONFIG_MONGODB_ADMINUSERNAME: admin
            ME_CONFIG_MONGODB_ADMINPASSWORD: password
            ME_CONFIG_MONGODB_URL: mongodb://admin:password@mongo:27017/
        depends_on:
            - mongo
    # backend:
    #     build: ../school-backend/.
    #     environment:
    #         MONGO_URL: "mongodb://localhost:27017/"
    #         PORT: 3001
    #         VSCREEN_URL: "http://localhost:3000"
    #         SCREEN_ID: 1
    #         MONGO_DB_NAME: "mainDB"
    #         SCREEN_TOKEN: "A6BBUGhDEvbybKQ52hR7jQn4"

